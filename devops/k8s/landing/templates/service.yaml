apiVersion: v1
kind: Service
metadata:
    name: internal-landing-service
    labels:
        task: landing
spec:
    type: NodePort
    ports:
        - port: 8080
          protocol: TCP
    selector:
        task: landing